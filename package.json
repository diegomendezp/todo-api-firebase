{
  "name": "server-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "test": "NODE_ENV=test jest  --verbose --silent",
    "test:watch": "NODE_ENV=test jest tests/todos-refactor.test.js --verbose --silent --watch"
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "firebase-admin": "^10.0.0",
    "jest": "^27.3.1",
    "mongoose": "^6.0.11",
    "morgan": "^1.10.0",
    "supertest": "^6.1.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
